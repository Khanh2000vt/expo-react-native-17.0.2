enum Password {
  CURRENT = "Current password",
  NEW = "New password",
  CONFIRM = "Confirm new password",
}

enum TypePassword {
  CURRENT = "current",
  NEW = "new",
  CONFIRM = "confirm",
}

export { Password, TypePassword };
