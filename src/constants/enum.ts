enum Input {
  HEIGHT = 58,
  MARGIN_TOP = 4,
  MARGIN_BOTTOM = 16,
}

enum Container {
  PADDING_LEFT = 25,
  PADDING_RIGHT = 23,
}

enum Button {
  HEIGHT = 56,
}

enum OtherProfile {
  OTHER,
  REQUEST_PENDING,
  INVITATION,
  FRIEND,
}

export { Input, Container, Button, OtherProfile };
